<h1>{{person.name}} is {{person.age}} years young</h1>

{{#each blogs as |blog| }}
<div class="card">
   <a href="/{{blog.id}}"> <h1 class="card-header">{{blog.title}}</h1></a>
    <h3>Creted by {{blog.user.username}}
</h3>
</div>
{{/each}}